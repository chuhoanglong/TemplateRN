import React, { memo } from 'react';
import { View, Text } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import { use{{properCase name}}Style } from './styles'

const _{{pascalCase name}}Screen = ({}) => {
const navigation = useNavigation();
const styles = use{{properCase name}}Style();

  return (
    <View>
      <Text>{{pascalCase name}} Screen</Text>
    </View>
  );
};

export const {{pascalCase name}}Screen = memo(_{{pascalCase name}}Screen);